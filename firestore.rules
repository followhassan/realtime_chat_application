rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Assessment rules: any signed-in (anonymous) user can use the room.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
